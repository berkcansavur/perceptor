{
  "name": "sandbox-ts",
  "version": "0.1.0",
  "private": true,
  "description": "Small DI sample app for testing repo-visualiser add/move-behavior tasks.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc",
    "start": "node dist/index.js"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
